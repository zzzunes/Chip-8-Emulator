#include <iostream>

int main() {
    // Create an instance of chip_8 and prompt the user for a ROM
    // Handle window drawing with something like SDL (maybe other file? window.cc?)
    // loop, chip_8::execute_instruction() <-- memory based
    return 0;
}

